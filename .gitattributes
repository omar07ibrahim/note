# Auto detect text files and perform LF normalization
* text=auto

# Frozen evaluation hashes must survive cross-platform checkouts byte-for-byte.
evals/lexical-v1/*.json text eol=lf
evals/lexical-v1/*.md text eol=lf
